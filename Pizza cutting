#my pizza cutting file

#include <stdio.h>
int main() {
    int n;
    while (1) {
        scanf("%d", &n);
        if (n < 0){
          break;
        }
        int pieces = n * (n + 1) / 2 + 1;
        printf("%d\n", pieces);
    }
    return 0;
}
